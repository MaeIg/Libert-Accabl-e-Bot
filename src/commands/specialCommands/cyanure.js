const cyanure = (message) => {
  message.channel.send(
    `Ca spamme beaucoup trop sur ce canal, ${message.author.username} a donc décidé d'en finir avec cette communauté oppressante.`
  );
};

export { cyanure };
